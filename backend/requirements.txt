# The native boundary shares the API service's provider/runtime dependencies
# during migration. Keep this file deployable as its own service.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
python-dotenv>=1.0.0
sarvamai>=0.1.0
livekit-api>=1.0.0
livekit-agents>=1.5.0
websockets>=13.0
